{
  "name": "round_robin",
  "version": "1.0.0",
  "description": "",
  "main": "multiServer2.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
